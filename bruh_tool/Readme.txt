📦 File Compressor / Decompressor (Huffman Coding)

A simple and interactive Streamlit web application that allows users to compress and decompress text-based files using Huffman Coding.
The app supports .txt and .py files for compression and generates a .zip file containing:

compressed.bin
codes.json

You can also upload this .zip file to decompress and retrieve the original text.

🚀 Features
✅ File Compression

Upload .txt or .py files

Huffman Coding–based compression

Displays compression statistics:

Original size (bits)
Compressed size (bits)
Compression ratio

Downloads a .zip containing:

compressed.bin
codes.json

🔄 File Decompression

Upload the .zip file generated by the compressor

Automatically extracts:

compressed.bin
codes.json

Restores the original text

Allows downloading the decompressed output as .txt

------------------------------------------------------------------------------------

The provided ZIP file will be extracted in which the report and the source code can be
found.
 For the code to run, the file of requirements must be run in the terminal to import all
the required libraries.
 Then the command streamlit run UI.py will be written in the terminal to access the
GUI. Upon writing the command, the local URL will automatically open in your
browser.
 If it doesn’t open automatically, the user can still open it by clicking on the network
URL provided in the terminal. 

Network URL: https://bruh-tool.streamlit.app/